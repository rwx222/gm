/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false, // NOTE: -> false to prevent double mount on first render (only happens in development mode)
}

export default nextConfig
